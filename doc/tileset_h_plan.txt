class CTK_Tileset
Job:Hold data and Render for tilesets

Constructors / Destructors:
Tileset()
Tileset(std::string filename ) //create tileset from .bmp file
~Tileset() //delocate memory

Functions:
bool load_from_file( std::string path) //load from file will delete prevouse data

void free() // delocate textre

void render( int x, int y, char clip) //render texture at x

Private Variables:
SDL_Texture* texture // actual texture
SDL_Rect tile_clips //diffrent sprites in texture 
						//moving from left to right
						//up to down
usigned int width and hieght;
unsigned int tile_width and tile_height

